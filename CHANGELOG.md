# 更新日志

本文件记录 NIE-SLA 控制面和前端的稳定版本。Agent 二进制使用独立的 `vX.Y.Z` 版本与 Release，不与应用版本混用。

## 1.0.22 - 2026-07-25

- 移除后台部署仓库与 GitHub Token 输入，应用更新不再要求用户提供敏感凭据。
- 一键部署仓库默认每 6 小时检查官方稳定版本；也可在 Actions 中无参数手动运行。
- 更新日志改为后台站内弹窗展示，版本卡片保持简洁。
- 用户仓库更新采用轻量应用测试、公开仓库安全扫描与 Wrangler dry-run，不再重复构建 Rust Agent。

## 1.0.21 - 2026-07-25

- 在线更新改用官方基准快照校验，兼容 Cloudflare 创建的无共同 Git 历史源码导入仓库。
- 更新前会比较当前部署与对应官方标签；除 `wrangler.jsonc` 外存在自定义代码时安全停止，避免静默覆盖。
- 同步继续保留用户原有 Cloudflare 绑定，并在提交前执行构建、完整测试和 Wrangler dry-run。

## 1.0.20 - 2026-07-25

- 新增后台“系统更新”卡片，显示当前版本、最新版本、发布时间和版本说明。
- 支持使用一次性 GitHub fine-grained token 触发部署仓库的受控更新工作流；Token 不落库、不写入环境变量，也不保存在浏览器中。
- 在线更新只接受官方 `app-vX.Y.Z` 稳定标签，并在推送前完成构建、完整测试和 Wrangler dry-run。
- 更新工作流兼容 Cloudflare 创建的独立源码导入仓库；同步前会校验当前官方基准，发现自定义代码时安全停止。
- 更新流程始终保留部署仓库现有的 `wrangler.jsonc`，避免覆盖用户的 D1、R2 与 Durable Object 绑定。
- 将“线路类型”统一改为“机器类型”，补充建站机、线路机、落地机、中转机、存储机、计算机和其他用途。
- 优化后台设置页三列布局及窄屏表单排版。

## 版本规则

- `app-vX.Y.Z`：Worker、同源前端和部署模板的应用稳定标签，仅供在线更新使用。
- `vX.Y.Z`：Rust Agent 二进制版本；对应 GitHub Release、校验清单和自动更新资源。
