# 更新日志

NIE-SLA 从 `1.0.38` 起使用正式稳定版本。应用、Worker 与 Rust Agent 共用同一个 `X.Y.Z`，正常迭代共同增加补丁位 `0.0.1`。

## 1.1.0 - 2026-08-02

- 版本号切换为十进制语义并直接跳到 `1.1.0` / `v1.1.0`，不再经过 1.0.68-1.0.99；应用、Worker、Rust Agent、前端、教程与公开发布版本保持同一数字。
- 后台备份预览与恢复增加低频 D1 限流（预览 10 次/分钟/来源 IP，恢复 3 次/5 分钟/来源 IP），避免管理会话泄漏时被高频滥用；正常低频备份操作不受影响。
- 后台任务请求超时提示补充“请求可能仍在服务端继续执行，请稍后刷新查看”，避免用户误以为任务一定未提交。
- 探针页任务状态自动轮询连续失败时显示非阻塞提示，恢复后自动消失；自动轮询继续安静重试，不再静默吞错。
- 单机运行 NQ/IP 解锁提交失败后也会刷新任务列表，避免失败结果与任务实际状态不一致。
- 前端所有本次变更模块的缓存键统一升至 `20260802-v1100`，发布后旧缓存不会继续加载过期模块。

## 1.0.67 - 2026-08-02

- 修复后台批量运行 NQ/IP 解锁时“确认排队”弹窗不退出、右下角无提示、刷新后部分 VPS 未开始的问题：Worker 批量创建任务改为每批 5 台并发，前端单机请求超时提升到 30 秒、批量请求提升到 60 秒，并在提交前先关闭弹窗、显示“正在排队”提示。
- 批量排队不再依赖长请求结束后才关闭 UI；请求成功或失败后都会刷新任务状态，避免旧客户端看到“提交中”卡死或部分任务未下发。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.67` / `v1.0.67`。

## 1.0.66 - 2026-08-02

- 修复批量 NodeQuality 在 `v1.0.63` 及更旧 Agent 上约 600 秒被外部 `timeout` 以 exit 124 终止的问题：Worker 对 NQ 任务重新下发 `timeout_sec: 3600` 作为旧 Agent 兼容上限，同时保持 NQ 排队过期仍为 7 天，不占用永久互斥位。
- `v1.0.64+` Agent 继续忽略 NQ 的 `timeout_sec`，慢速设备可一直运行到完成；深度模式（`v`）在低配机器上仍可能持续较久，建议批量任务优先选择快速硬件模式（`f`）。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.66` / `v1.0.66`。

## 1.0.65 - 2026-08-02

- IP 解锁完整报告自动裁剪上游脚本尾部的赞助商广告（SPONSOR、IPWO、Lisahost、RapidProxy、YINNET、VMRack、IPPeak、Swiftproxy 等）：Agent 在结果回传前、Worker 在存储前、后台在渲染前都按“今日IP检测量 / 总检测量 / 感谢使用xy系列脚本 / 报告链接”标记截断，并保留 ANSI 颜色与完整报告正文。
- 无法识别尾部标记的旧报告会从 `TERM environment variable not set.` 或全大写广告横幅处回退截断；报告仍限制为 64 KiB，公共状态页显示效果不变。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.65` / `v1.0.65`。

## 1.0.64 - 2026-08-02

- 后台运行 NodeQuality 时可在确认弹窗分别选择 HardwareQuality（`y/f/v/n`）、IPQuality（`y/n`）、NetQuality（`y/l/n`）与回程路由（`y/n`），默认 `f/y/y/y`；单机与批量任务都随请求提交选项，Agent 按白名单映射为固定 stdin，不开放任意命令或脚本参数。
- NodeQuality 任务取消外部 `timeout` 包装，慢速设备可一直运行到完成，不再因下载 Geekbench 或慢盘阶段被 exit 124 提前终止；IP 解锁仍保留 600 秒上限。
- 排队过期保留 7 天加 900 秒宽限，避免未领取的 NQ 任务永久占用单任务互斥位；Worker 新增 `agent_tasks.options` 字段，旧库通过 Schema 迁移自动补齐。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.64` / `v1.0.64`。

## 1.0.63 - 2026-08-02

- 系统调试日志改为可折叠卡片并默认收起，后台仍可一键刷新查看最近 200 条带操作 IP 的 Debug 记录。
- Latency 管理中的 Cloudflare 内置来源固定保留、不可删除，并与其他第三方节点一样支持自定义曲线颜色；颜色同时应用到公共状态页图表。
- 修复 NQ 与 IP 解锁渲染：媒体状态“仅自制”恢复黄色，源报告标记为“解锁”的 ChatGPT 等服务即使使用 DNS 方式也保持绿色。
- 修复批量运行 NQ/IP 解锁时目标搜索框无效的问题；Worker 按规范化与原始 ID 双重匹配，支持包含 `&` 等特殊字符的 VPS ID 正常下发任务。
- NodeQuality 硬件质量检测改为快速模式（`f`），避免慢速设备在下载 Geekbench 等依赖时长时间卡住并触发 exit 124 超时。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.63` / `v1.0.63`。

## 1.0.62 - 2026-08-02

- 后台 IP 解锁任务详情恢复展示完整 IPQuality 终端报告：基础信息、IP 类型属性、风险评分、风险因子、流媒体解锁检测、邮局连通性及黑名单检测等段落按原报告排版显示，流媒体段落仍自动转换为与 NQ“IP质量”一致的四列网格。
- Agent 的 IP 解锁固定命令由 `-4 -j -n -p` 改为 `-4 -n -p`：去掉 `-j` 后脚本不再只输出最终 JSON，完整正文会随任务结果回传；`-p` 继续禁用 Report.Check.Place 公开链接，保持隐私模式。
- Agent 与 Worker 都把完整报告限制为 64 KiB，Worker 在存储前清洗控制字符并保留 ANSI SGR 颜色；任务错误与可折叠原始输出在后台详情中继续保留。

## 1.0.61 - 2026-08-02

- 后台 IP 解锁任务详情不再直接展示原始 JSON，改为与 NodeQuality 报告第二页签“IP质量”一致的流媒体解锁网格；成功、失败、排队与运行中的任务详情都复用同一套 NQ 风格弹窗。
- 原始输出摘要保留在可折叠的“原始输出”区域，任务错误仍直接显示；前台公共状态页的显示效果保持不变。
- 手机端 NQ 页签等分居中、后台 Debug 操作日志与 30 天自动清理、原生 xterm NQ 报告、固定公益图床与任务安全边界继续保留。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.61` / `v1.0.61`。

## 1.0.60 - 2026-08-02

- 手机端 NodeQuality 报告弹窗的四个页签改为等分并居中显示，基本信息、IP质量、网络质量、回程路由在 320px 起的窄屏上也能一屏可见，不再被挤到左侧或需要横向滚动。
- 后台“设置 → 系统”新增 Debug 级别操作日志：记录操作 IP、方法、路径、状态、操作者与操作摘要；登录、设置、探针配置、Agent 任务、备份恢复、主题和安全操作均纳入记录。
- Debug 日志只记录 pathname，不记录查询串、Session、Authorization、Cookie 或请求体；定时维护每小时自动清理 30 天前的日志，并提供后台只读查询接口。
- 保留原生 xterm NQ UI、固定公益图床、D1/R2、备份恢复和任务安全边界；应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.60` / `v1.0.60`。


## 1.0.59 - 2026-08-02

- NodeQuality 深度检测总时限从 1800 秒提高到 3600 秒，避免低配置或慢盘 VPS 在 HardwareQuality 的 Crystal/ATTO 阶段尚未进入网络质量与回程路由就被提前终止。
- IP 解锁继续保持 600 秒上限；NodeQuality 固定输入仍为 `v/y/y/y`，两个任务继续由 root-only Agent Manager 直接执行，不恢复低权限 sandbox、`nstatus-task`、user namespace 或 `no_new_privs`。
- 普通指标采集继续由低权限 `nstatus` 服务运行；固定 action、同源脚本镜像、SHA-256、私有目录、符号链接拒绝、清空环境、固定 PATH、输出上限和结果净化边界保持不变。
- 固定公益图床、原生 xterm NQ UI、D1/R2 绑定和备份恢复行为不变；应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.59` / `v1.0.59`。

## 1.0.58 - 2026-08-02

- 修复 NodeQuality/IP 解锁任务“总任务成功但延迟全为 0、回程为 NoData”的权限回归：这两个固定诊断不再降权到 `nstatus-task` 或进入 user namespace，而是由已强制 root 运行的 Agent Manager 直接执行，以保留 raw socket、路由探测和必要系统工具能力。
- 普通指标采集继续由低权限 `nstatus` 服务运行；特权 Manager 仍只接受 `nodequality` 与 `ip_unlock` 两个固定 action，不开放任意命令。
- 固定任务继续要求受管镜像、精确 SHA-256、root 上下文、私有随机目录、无符号链接、清空环境、固定 PATH、超时、输出上限和结果净化；取消的仅是会让诊断数据失真的低权限/user namespace 层。
- 新安装不再创建 `nstatus-task`；卸载流程仍兼容清理旧版本遗留账户。应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.58` / `v1.0.58`。

## 1.0.57 - 2026-08-01

- NodeQuality 报告改为双通道捕获：继续从隔离任务私有目录读取五类日志，同时在上游上传和清理前用有界记录回送给 Agent；任一通道可用都能保存本站报告。
- 回送记录逐字段限制为 24 KiB、总块限制为 180 KiB，严格校验边界和 base64；坏数据只会被忽略，不会执行报告内容或放宽任务隔离。
- 即使 NodeQuality 官方报告接口返回空或损坏 ZIP，本站仍优先使用 Agent 本机捕获的基本信息、IP质量、网络质量和回程路由生成报告与固定公益图床图片。
- 保留 `1.0.56` 恢复的原生 xterm SVG、无本站标题栏、图片优先与手机整图等比缩放；应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.57` / `v1.0.57`。

## 1.0.56 - 2026-08-01

- 精确恢复 `2026-07-29 22:19` 与 `2026-07-31 04:22` 使用的 NodeQuality xterm 风格 SVG：连续终端排版、原始 ANSI 前景/背景色、方块进度条、双倍像素密度和黑灰背景，不再添加本站标题栏。
- 网络质量与回程路由重新优先显示固定公益图床保存的同源代理图片；原始文本继续保留，并仅在图片加载失败时作为安全兜底。
- 前台与后台继续共用同一报告弹窗；桌面和 390px 手机视口保持页面、弹窗与活动面板无横向溢出，报告按原始整图比例缩放。
- 公益 Broker、固定 S3 空目录上传、Worker-only Secret、D1/R2、NodeQuality 执行参数、BusyBox 降权与 Linux namespaces 边界保持不变。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.56` / `v1.0.56`。

## 1.0.55 - 2026-08-01

- 网络质量与回程路由恢复此前的 NodeQuality ANSI 报告界面，不再优先展示带额外标题栏的深蓝自制 SVG。
- 前台与后台继续共用同一报告弹窗；手机端保持终端列对齐，超宽内容只在当前报告分段内横向滚动，不撑宽页面或弹窗。
- 固定官方公益图床继续上传、保存并通过本站代理隐藏真实上游地址；仅在旧数据缺少原始报告内容时使用图片兜底。
- NodeQuality 执行参数、报告采集、BusyBox 直接降权、`no_new_privs`、Linux namespaces 与私有任务目录边界保持不变。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.55` / `v1.0.55`。

## 1.0.54 - 2026-08-01

- 后台点击“NodeQuality · 已完成”时直接打开与公开状态页一致的结构化报告弹窗，不再展示通用任务输出和原始 JSON。
- 成功的历史 NQ 任务不再依赖旧 `report_saved` 标志选择弹窗；若目标确实没有本站完整报告，仍保留 NQ 对话框并明确提示重新运行，可用时提供经过域名与路径校验的原报告链接。
- 手机端恢复 NodeQuality 终端的原始空格、分隔线和字段列对齐；超宽 ANSI、报告简介和网络分段只在当前局部面板横向滚动，弹窗与页面宽度保持受控。
- 前台与后台共享同一 NQ 报告渲染器，固定官方公益图床、BusyBox 直接降权、`no_new_privs`、Linux namespaces 与私有任务目录边界保持不变。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.54` / `v1.0.54`。

## 1.0.53 - 2026-08-01

- 修复 NodeQuality 报告内容未保存、网络质量与回程路由图片未进入固定公益图床的问题：Agent 在调用上游 `upload_result` 前先把五类结果日志复制到私有捕获目录，避免上游上传或清理阶段使本地内容不可用。
- 任务父目录继续保持 root 所有的 `0711`，随机任务目录与 NQ 捕获目录继续由 `nstatus-task` 独占 `0700`；捕获时序调整不放宽沙箱读取边界。
- Worker 在 Agent 只返回外部报告链接、没有可保存内容时记录明确的图片上传错误，不再以 `image_upload: null` 隐藏未执行原因。
- `1.0.52` 的 Alpine/BusyBox IP 解锁实机回归已通过；本补丁继续保留直接降权、`no_new_privs` 与 Linux namespaces 隔离。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.53` / `v1.0.53`。

## 1.0.52 - 2026-08-01

- 完整修复 NodeQuality/IP 解锁任务在宿主降权后的文件访问：root 管理的任务父目录改为仅可穿越、不可列举的 `0711`，每个随机任务目录继续保持 `nstatus-task` 独占的 `0700`，脚本和兼容 helper 不再因父目录 `0700` 返回 Permission denied。
- 保留 `1.0.51` 移除 GNU `setpriv` 参数依赖的实现，Agent 仍在 `exec` 前清空补充组、切换非 root 身份、设置 `PR_SET_NO_NEW_PRIVS` 并进入 Linux namespaces；任一步失败都不会回退宿主 root。
- 使用真实 Alpine Linux 3.23 / BusyBox 1.37 节点执行固定 IP 解锁任务，验证任务领取、脚本启动和结果回传。
- 固定公益 NQ 图床链路、双动作任务详情与生产 D1/R2 绑定保持不变。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.52` / `v1.0.52`。

## 1.0.51 - 2026-08-01

- 修复 BusyBox 节点运行 NodeQuality 或 IP 解锁时，因 BusyBox `setpriv` 不支持 GNU `--reuid` / `--regid` 参数而在脚本启动前失败的问题。
- Agent 改为在子进程 `exec` 前直接清空补充组、切换到非 root `nstatus-task` 身份并设置 `PR_SET_NO_NEW_PRIVS`，随后进入原有 user/mount/pid/ipc/uts namespaces；任何隔离步骤失败仍会拒绝任务，不会回退宿主 root。
- 后台按每台 VPS、每种动作分别保留最新 NQ 与 IP 解锁状态和错误，连续批量运行两类任务后仍可分别打开详情。
- 固定公益 NQ 图床链路、受限 Broker 协议与生产 D1/R2 绑定保持不变。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.51` / `v1.0.51`。

## 1.0.50 - 2026-08-01

- 一键部署与普通手动部署固定使用官方公益 Broker 生成 NodeQuality 网络质量与回程路由图片，不再要求用户填写自己的图床地址或 Token，也不允许非官方实例通过本地 Secret 覆盖这条链路。
- 公益接口只接受一到两个有界 `network`/`route` 文本标签，拒绝任意 SVG、文件、额外字段、重复标签和超长正文；官方服务端重新生成美化 SVG，再以固定 S3 渠道和空目录上传。
- Broker 使用 D1 同时执行每来源和全局小时限流，数据库故障时失败关闭；所有成功和错误响应均禁止缓存，官方实例缺少私有图床 Secret 时不会递归请求自身。
- 共享图床地址与 Token 继续只保存在开启 Broker 的官方 Worker Secret，不进入源码、前端、备份、Fork、教程或 Broker 请求；公开仓库只固定公益接口地址和受限协议。
- 应用、Worker、Rust Agent、前端与教程版本同步至 `1.0.50` / `v1.0.50`。

## 1.0.49 - 2026-07-31

- 修复 `cftz` 把 Agent Bearer Token 错发为后台 Session 的问题；Agent 更新与 Ping 目标使用 Bearer，后台操作继续使用短期 `x-admin-session`，并加入真实 HTTP 握手回归测试。
- Manager 任务输出与脚本进程均有强制期限；systemd/OpenRC 在 Manager 不活跃时恢复校验更新通道，指标只有收到 Worker 明确 `ok: true` 才会从本地队列确认删除。
- NodeQuality 与 IP 解锁入口使用随站点发布的已审计快照和固定 SHA-256，并在宿主 `nstatus-task` 低权限身份与独立 user/mount/pid/ipc/uts namespace 中执行；隔离能力缺失时拒绝执行。
- TOTP、可恢复 Agent Token、通知凭据和恢复快照改用长期独立加密材料；后台新增只显示配置状态的迁移检查，支持 `PREVIOUS_ENCRYPTION_KEY` 轮换且不改变在线 Agent 的 `token_hash`。
- 公共与后台请求增加默认超时，动态数值输出补充转义；Linux 网络统计排除容器 bridge/veth 等虚拟接口，保留物理、OpenVZ 与 VPN 接口。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.49` / `v1.0.49`。

## 1.0.48 - 2026-07-31

- 修复从旧备份恢复后，包含大写字母或 `&` 等特殊字符的历史 VPS ID 无法生成 Agent 部署命令的问题。
- 部署命令继续绑定后台所选的原始目标，Agent 运行身份则使用安全规范化 ID，不迁移或重命名既有探测数据。
- 增加 Agent ID 冲突保护；两个历史目标若规范化为同一身份会明确拒绝部署，避免凭据或监控数据串号。
- 增加大小写、特殊字符、一次性票据兑换和冲突拒绝的完整回归测试。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.48` / `v1.0.48`。

## 1.0.47 - 2026-07-31

- 修复 Cloudflare 一键部署的同源 Worker 在 `Referrer-Policy: no-referrer` 下无法推导 Agent 安装地址，导致“部署 Agent”短暂显示生成中后消失、剪贴板为空的问题。
- 同源集成部署现在会在没有显式 Agent 域名、`Origin` 和 `Referer` 时，安全回退到当前公开 HTTPS Worker Origin；前后端分离部署与自定义 Agent 域名的优先级保持不变。
- 增加与生产绑定形态一致的回归测试，覆盖无公开域名变量、无来源请求头的安装命令生成。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.47` / `v1.0.47`。

## 1.0.46 - 2026-07-31

- 修复部分 macOS 浏览器中 Clipboard API 永久等待，导致“部署 Agent”一直停留在生成中且剪贴板为空的问题；超时或权限拒绝时会自动使用兼容复制，并在双路径失败时恢复按钮并显示错误。
- 新受保护备份把每节点 Agent Token 放入备份密码加密的数据包，恢复时使用新部署的加密材料重新封装；迁移到全新 Worker/D1 后，已安装节点无需逐台重装。普通可分享备份仍不包含凭据。
- 后台 Agent 设置增加自定义连接域名；经过校验的公网 HTTPS Origin 统一用于 Agent/Latency 安装命令、API 和自动更新下载，并随便携配置备份恢复。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.46` / `v1.0.46`。

## 1.0.45 - 2026-07-31

- 修复 Cloudflare WebCrypto 不支持 310,000 次 PBKDF2，导致普通备份恢复前的 R2 加密快照和敏感备份不可用的问题；新 envelope 使用受支持的 100,000 次并严格校验参数。
- replace 恢复的清理、写入与回滚改为最多 50 条一批的 D1 batch，避免较大 JSON 备份耗尽单请求子请求预算。
- Worker Static Assets 启用 `404-page` 回退，未知浏览器路径会展示品牌 404 UI，不再返回空 body。
- 使用 44 个目标、5 个 Ping 目标、2 个外部延迟节点和 18 条设置的真实普通备份完成全新 D1/R2 replace 恢复验证。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.45` / `v1.0.45`。

## 1.0.44 - 2026-07-31

- 修复自定义后台入口下相对资源地址可能导致管理模块加载失败、点击登录没有任何反馈的问题；后台资源改为站点根路径，并增加独立启动失败提示。
- 公开一键部署构建从 `update-manifest.json` 固定 Agent Release 版本并直接下载、校验资产，不再调用容易被共享出口限流的 GitHub Release API。
- 明确公开仓库是开源用户唯一分发入口；生产 Agent 仍从已部署站点的 `/bin` 静态资产安装和更新。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.44` / `v1.0.44`。

## 1.0.43 - 2026-07-31

- 新增最多 50 台 VPS 的批量 NodeQuality 与 IP 解锁任务排队，逐台验证在线状态、能力和任务互斥。
- 新增与状态页视觉一致的 404 页面，隐藏 NQ 与后台弹窗的视觉滚动条并保留滚动能力。
- 后台设置页改为双列独立堆叠，避免不等高卡片造成大块空白。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.43` / `v1.0.43`。

## 1.0.42 - 2026-07-31

- TCP Ping 默认间隔恢复为 20 秒，后台可配置范围调整为 5-300 秒；数据库中遗留的 1 秒配置会自动回退为 20 秒。
- Agent 网络探测保留 TCP 与 HTTP/HTTPS，移除 ICMP 实现、依赖和 Linux `CAP_NET_RAW` 权限；遗留 ICMP 目标不会再下发给 Agent，可在后台删除。
- 内置 TCP Ping 图表移除丢包轨道、标记和悬浮丢包提示，目标摘要仍显示按原始样本计算的丢包率；`include_loss`、`loss_series`、R2 原始数据和时序导出接口保持兼容。
- 公开 Ping 查询恢复每目标 2000 点硬上限，前端继续使用 1 小时 360 点、6 小时 360 点和 24 小时 480 点的有界曲线。
- 应用、Worker 与 Rust Agent 版本同步至 `1.0.42` / `v1.0.42`。

## 1.0.40 - 2026-07-30

- 保持现有目标字段与前端布局不变，通过 `tcp://`、`http://`、`https://` 和 `icmp://` scheme 增加 Agent 侧 TCP、HTTP 与 ICMP 探测；无 scheme 的 `主机:端口` 继续按 TCP 处理。
- Agent 在请求目标时声明协议能力，旧 Agent 仍只会收到 TCP 目标，支持滚动升级；Linux 安装、更新和回滚为 ICMP 设置受限 `CAP_NET_RAW`，不修改全局 `ping_group_range`。
- `/api/v1/pings` 增加可选 `include_loss=1`，以紧凑 runs 无损返回所有原始丢包时间；现有降采样曲线、统计字段和前端控件保持兼容，连续一秒丢包直接绘制为时间区间。
- 增加默认关闭的 Worker 侧 VictoriaMetrics 与 InfluxDB 小时批量导出。R2 继续作为权威默认存储，导出地址和 Token 仅由 Worker Secret 提供，远端故障不会阻塞 Agent 上报或 R2 归档。
- Rust Agent 更新至 `v1.0.40`，五种 Linux 静态架构产物、SHA-256 校验和及完整安装引导哈希链均在本地生成并验证。

## 1.0.39 - 2026-07-30

- Ping 管理支持 1-300 秒全局采样间隔，Agent 在运行期间自动应用后台配置。
- 1 秒模式最多启用 5 个 Ping 目标；Worker 在保存配置、创建目标和重新启用目标时统一校验。
- Agent 改用紧凑 Ping 序列与 5000 点有界批次；Durable Object 使用 5 分钟紧凑分块，完成小时仅写入一次 R2。

## 1.0.38 - 2026-07-30

- 修复后台系统更新在官方清单上游返回 HTTP 429 时整卡加载失败的问题；更新检查现在使用六小时缓存、并发合并、失败退避和随部署打包的可信清单回退。
- 官方清单恢复后仍会继续检查更高版本；降级期间只展示当前部署或上次成功缓存的版本，不暴露上游地址，也不要求用户配置 GitHub Token。
- 应用、Worker 与 Rust Agent 结束独立 Beta 编号并统一为 `1.0.38` / `v1.0.38`，以后每次正常迭代共同增加 `0.0.1`。
- Rust Agent 在 macOS 本地完成五种 Linux 架构构建、SHA-256 校验和及完整安装引导哈希链更新。

## 0.24.0-beta.31 - 2026-07-30

- 修复根文件系统重复绑定挂载导致磁盘总容量虚高的问题，并按 Linux 叶子块设备统计磁盘 IO，避免 LVM、device-mapper 与其底层设备重复计数。
- Agent 离线样本队列改为临时文件原子替换并主动落盘；上传失败会保留队列重试，退出或重启前先刷新，且拒绝类型不正确的队列 JSON。
- Worker 对指标、状态、告警、主题、备份和代理边界进行全面输入规范化与失败处理；部分数据异常会明确标记，不再静默生成误导性结果。
- 公开一键部署工具链升级到已修复已知开发依赖漏洞的 Wrangler 版本，保持构建与 dry-run 校验链可复现。
- NodeQuality 上传固定使用 S3 且目录留空，上传地址和 Token 仅存在于 Worker Secret；浏览器、公开 API、普通备份和公开源码只接触本站同源图片代理。
- 修复手机端 NQ 长行、图片宽度和滚动层级，以及后台弹窗初始焦点、焦点循环、关闭后焦点恢复和小屏内部滚动；同步更新静态资源缓存键。
- Rust Agent 更新至 `v1.0.37`，本地完成五种 Linux 架构构建、SHA-256 校验和及完整安装引导哈希链固定。

## 0.24.0-beta.30 - 2026-07-30

- 修复手机端 NodeQuality 基本信息与 IP 质量仍按桌面终端宽度排版，导致正文右侧大面积隐藏、触摸横向滚动层级混乱的问题。
- NQ 弹窗外层现在只负责纵向滚动，ANSI 页签独立承接极端长行；标准终端行在普通手机与紧凑 iPhone 上分别使用固定 8px、7px 等宽排版，并继续保留原始列对齐。
- 网络质量与回程路由图片在手机端使用全部可用宽度，公开页和后台共用相同约束；CSS 缓存键同步更新，避免上线后继续命中旧样式。
- 新增移动端宽度、滚动边界、断点字号和图片占宽回归断言；Rust Agent 保持 `v1.0.36`，无需重新安装或构建 Agent。

## 0.24.0-beta.29 - 2026-07-29

- NQ 图片上传改为 Worker 内部固定策略：上传渠道强制为 `s3`，上传目录留空，旧数据库值及已废弃的 `NQ_IMGBED_CHANNEL`、`NQ_IMGBED_FOLDER` 均不能覆盖。
- 移除后台 NQ 图床卡片及读取、保存、测试三条 HTTP API；上传地址、Token 和可选渠道名只从 Worker Secret 读取，旧版 D1 密文仅保留只读迁移兼容。
- 公开 NQ JSON 和前端 DOM 只使用 NIE-SLA 同源图片代理，不再暴露上游图床域名或原始图片地址；上传错误、重定向、响应正文与普通备份继续保持脱敏。
- 新增固定 S3、空目录、旧配置不可覆盖、上游地址隐藏、敏感备份隔离和旧 D1 解密回退测试；Rust Agent 保持 `v1.0.36`，无需重新安装或构建 Agent。

## 0.24.0-beta.28 - 2026-07-29

- 修复 Rust Agent 将根文件系统与 `/etc/hosts`、`/etc/hostname`、`/etc/resolv.conf` 等同设备绑定挂载重复相加，导致约 14 GB 的 VPS 被显示为约 54 GB 的问题。
- 磁盘容量和使用率现在以系统根文件系统为准；Windows 优先系统卷，缺失根挂载信息时回退到 Agent 所在卷，不再把互不相关或重复的挂载点合并成一个虚假容量。
- 新增重复绑定挂载、最长匹配挂载点和容量换算回归测试，并在 macOS 本地完成 Rust 全量测试、严格 Clippy 与 Linux 多架构静态构建。
- Rust Agent 更新至 `v1.0.36`，现有 Linux Agent 会通过校验后的自动更新获取；新数据上报后前端磁盘容量会自动恢复为正确值。

## 0.24.0-beta.27 - 2026-07-29

- NodeQuality 网络质量与回程路由图床图片改为与原报告一致的 AdventureTime 终端主题，去除额外标题栏，并使用 14px 等宽排版、对称边距与 2 倍清晰度输出。
- 完整解析 ANSI 前景色、背景色、反色、粗体、暗色、斜体、下划线、删除线、256 色和 RGB 色；Braille 图表由 SVG 直接绘制为连续方格，不依赖浏览器或图床服务器字体，避免退化为圆点。
- Rust Agent 网络请求改为 IPv4 优先；仅在 DNS、连接、超时或 IO 等传输层失败时自动回退 IPv6，HTTP 认证与服务端错误不会重复请求，修复部分 VPS 因不可达 IPv6 长期无法上报和更新的问题。
- 上传失败后的积压数据严格遵守退避周期，不再因样本超过批次上限而约每 4 秒重复请求，降低网络故障期间的 Workers 请求消耗。
- Linux 安装程序现在必须同时确认主服务、任务服务存活且首包上报成功才返回安装完成；失败时输出经过 Token 脱敏的诊断信息，不再把“服务已启动”误报为“Agent 可用”。
- Rust Agent 更新至 `v1.0.35`，现有 Linux Agent 会通过校验后的自动更新获取，无需逐台重新安装；已生成的旧 NQ 图片不会自动重绘，重新运行 NQ 后生成新样式。

## 0.24.0-beta.26 - 2026-07-29

- 修复 Cloudflare Workers 运行时不支持 `fetch(..., { redirect: "error" })`，导致 NQ 图床请求在发送到 `images.example.com` 之前直接抛出 `TypeError` 的问题。
- 图床出站请求改用 Workers 支持的 `redirect: "manual"`，并在收到任何 `3xx` 时主动拒绝，继续防止 Token 被重定向到其他主机。
- 使用 Cloudflare 远程开发运行时复现并验证：同一请求现在可以正常到达图床并返回上游 HTTP 状态；本机与远程链路的 API 地址、TLS 和请求字段保持一致。
- 继续保留 30 秒超时、分级错误提示、响应体限制、公开 HTTPS 校验与文本报告回退；Rust Agent 仍为 `v1.0.34`，无需更新。

## 0.24.0-beta.25 - 2026-07-29

- NQ 图床上传严格对齐 CloudFlare ImgBed 官方 `POST /upload` 文档：保留 `multipart/form-data`、Bearer Token、渠道、渠道名称、目录及数组响应兼容，Telegram 渠道明确使用非压缩文件模式。
- 上传等待窗口由 10 秒调整为 30 秒，适配 Telegram 上传、文件登记和可选内容检测链路；普通 NQ 报告与现有前端展示保持不变。
- 测试上传不再把所有图床错误折叠成“服务器内部错误”，现在分别提示 Token 无效、缺少 `upload` 权限、渠道配置、接口地址、限流、上游故障与超时，同时继续隐藏响应正文、内部地址和 Token。
- 后台与中英文文档明确要求使用 Token 创建成功时返回的完整值；CloudFlare ImgBed Token 列表之后显示的截断值不能用于上传。
- Rust Agent 版本保持 `v1.0.34`，本次无需重新安装、自动更新或重新构建 Agent。

## 0.24.0-beta.24 - 2026-07-29

- 后台新增兼容 `MarSeventh/CloudFlare-ImgBed` 的 NodeQuality 图床：网络质量与回程路由可渲染为自包含 SVG 后上传，支持渠道、渠道名称和目录；API Token 使用 AES-GCM 加密保存在 D1，不返回浏览器或 Agent，上传失败自动回退文本报告。
- NQ 图床只允许公开 HTTPS `/upload` 接口，限制请求时间、响应体、报告行列与 SVG 代理 CSP；设置页提供独立测试上传，备份恢复保留非敏感配置与加密凭据边界。
- Agent Metrics 与 TCP Ping 增加按节点隔离的 Durable Object 小时缓冲：5 分钟上传频率、API 与图表点保持不变，当前小时直接读取，结束后每小时合并写一次 R2，大幅降低 Class A 操作。
- 30 天 SLA 快照优先读取随探测增量维护的 R2 日汇总，仅在单个目标状态落后时回查 D1；凭据活跃时间按 6 小时节流，完整 R2 快照改为 5 分钟一次，公开状态仍覆盖分钟级最新结果。
- 流量统计继续随每次 Agent 报告实时显示并参与报警，D1 周期行最多每 30 分钟落盘；公开 VPS 不再写入未使用的 Agent 心跳日账本，持久探测调度由单例 Durable Object 串行执行。
- 新增 100 台 VPS 免费额度回归预算：默认 5 分钟上报、全部开启流量统计并配置 2 个 External Latency Agent 时，估算约 78,720 Workers 请求/天、95,400 D1 写入行/天和 210,240 R2 Class A/月；实际仍需为公开访问、重试和运维操作设置用量告警。
- Rust Agent 版本保持 `v1.0.34`，本次无需重新安装或重新构建 Agent。

## 0.24.0-beta.23 - 2026-07-29

- 恢复独立第三方主题 ZIP 管理：支持上传、列表、启用、停用、删除和恢复原版；所有新上传及同 ID 覆盖更新都默认停用，必须由管理员重新确认，插件与通用扩展接口继续关闭。
- 支持 CSS 与 Canvas 两种主题。CSS 只能覆盖公开页样式；Canvas 在 `sandbox="allow-scripts"` 且无 `allow-same-origin` 的 iframe 中运行，只能经消息桥读取 `status:read` 白名单数据，不能访问后台会话、主页面 DOM 或主动联网。
- Worker 增加主题 ZIP 压缩/解压体积、文件数、单文件、路径深度、Unicode 规范化、重复路径、扩展名、Manifest、SemVer、权限和 SHA-256 校验；主题资源单独存入 R2，并兼容早期主题注册表。
- 公开页首屏先读取主题注册表，避免原版闪现后再切换；主题文件超时或加载失败时自动回退原版。后台主题页使用站内确认弹窗，并适配桌面与手机。
- 新增中英文第三方主题规范与 CSS/Canvas 可运行示例，明确消息协议、发布、无障碍、移动端、备份及 R2 迁移边界。
- 修复 NodeQuality 网络质量章节标题与同一行首组省份/测速列没有拆分，导致内容跑进标题上方的问题。
- Rust Agent 版本保持 `v1.0.34`，本次无需重新安装或重新构建 Agent。

## 0.24.0-beta.22 - 2026-07-28

- NodeQuality 网络质量报告按 BGP、本地策略、接入信息、三网延迟、回程概览和测速等章节拆分；宽终端表格改为对应章节内独立横向滚动，不再需要滚动到整份报告底部才能查看右侧内容。
- 回程路由改为“线路标题、地理与自治系统路径、逐跳节点”响应式结构，完整保留跳数、延迟、IP、ASN、网络和地理位置；长 IPv6 与位置名称在桌面和手机上自动换行。
- 回程延迟按绿色、黄色和红色继续区分，原始线路色块保持不变；无法结构化的少数内容仍以安全转义后的终端格式降级展示。
- 网络质量章节移除深色标题横条，仅用透明标题、字重和低对比度细线分隔，避免压过报告原有的终端视觉。
- 页签切换会同时复位外层报告、ANSI 区域和章节滚动位置；基本信息、IP 质量及原图报告保持现有布局和交互不变。
- Rust Agent 版本保持 `v1.0.34`，本次无需重新安装或重新构建 Agent。

## 0.24.0-beta.21 - 2026-07-28

- 后台成功完成的 NodeQuality 任务会直接加载 Worker 已保存的结构化报告，不再把任务 JSON 当作报告展示；加载失败时保留任务诊断信息，方便定位 Agent 或存储问题。
- 后台与公开前端共用 NodeQuality 四页签交互，补齐桌面与移动端报告样式、页签滚动复位、遮罩关闭和 `Esc` 关闭行为。
- 管理员账号密码更新改用适合 Cloudflare 免费 Worker CPU 配额的密码派生参数，修复请求已到达 Worker、但在 D1 写入前因 CPU 超限而无提示失效的问题；既有高迭代凭据继续兼容读取。
- 管理员凭据更新成功后仍会撤销全部既有会话并立即返回登录页，必须使用新账号密码重新登录。
- NodeQuality 四页签内容来自 Agent 在官方脚本清理前捕获的原始报告数据；第三方网站的“提交”和“复制 NodeSeek 格式”仍属于其站内流程，NIE-SLA 不伪装为官方提交产物。
- Rust Agent 版本保持 `v1.0.34`，本次无需重新安装或重新构建 Agent。

## 0.24.0-beta.20 - 2026-07-28

- NodeQuality 结果只接受 `https://nodequality.com/r/<报告ID>` 正式链接，拒绝脚本入口、API 地址、首页地址和无效旧任务，避免后台展示错误报告。
- Rust Agent 在官方脚本清理临时文件前复制基本信息、IP 质量、网络质量和回程路由日志；Worker 保存结构化四页签报告，并从报告正文提取测试时间。
- TCP Ping 与 Latency 图表统一为连续延迟线和黄色半透明丢包峰；每个目标或探测来源在图表上方同时显示当前延迟与独立丢包率。
- Ping 目标和 Latency 来源支持相同的点击筛选：首次点击单独显示，继续点击追加曲线，全部重新选中时恢复完整图表；移动端保留横向滚动摘要卡。
- 历史超过一秒探测预算的 Latency 伪成功记录不再撑高纵轴，统一按丢包展示；Cloudflare 漏检记录不会误算成网络丢包。
- Rust Agent 更新至 `v1.0.34`，现有 Linux Agent 通过校验后的自动更新获取，无需逐台重新安装。

## 0.24.0-beta.19 - 2026-07-28

- 修复管理员账号密码更新写入后重复执行高成本密码派生，导致 Worker 超时、前端恢复原样且新凭据未落库的问题；启用 TOTP 时验证码改为明确必填，失败原因会持续显示并提供重试。
- 管理员账号更新限流提高到合理的重试窗口；成功后撤销全部既有会话、清理本地 Session 并立即返回登录页，要求使用新账号密码重新登录。
- 修复 NodeQuality 已生成正式报告却被上游清理阶段固定退出码 `1` 误判失败的问题；Agent 改为保留输出尾部并折叠终端进度动画，确保报告链接不会被大量 Fio 输出挤出缓冲区。
- 后台 VPS SLA 概览新增最近 30 天每日整体 SLA 趋势、99% 参考线和节点数提示；逐台节点按 SLA 从高到低排列，未知数据保持在末尾。
- 公开 VPS 列表增加低对比度分隔线，并延展底部 SLA 格子区域以改善视觉重心；桌面与移动端保持现有信息结构和交互不变。
- Rust Agent 更新至 `v1.0.33`，现有 Linux Agent 继续通过校验后的自动更新获取，无需逐台重新安装。

## 0.24.0-beta.18 - 2026-07-28

- 后台设置改为外观、通知、Agent、安全、系统五个二级页面，并记住上次分类；原空白“系统指标 / VPS 信息”替换为最近 30 天全体 VPS SLA 概览，不增加额外 Worker 请求。
- 管理员账号密码更新增加写入后回读验证，成功后撤销全部会话并立即要求重新登录；登录限流改为 D1 持久限制，避免跨实例绕过。
- VPS 批量设置改为“选择 VPS、填写字段、确认应用”三步站内弹窗；NodeQuality、IPv4 解锁及失败详情不再使用浏览器原生确认框。
- 第三方 Latency 历史改为 R2 六小时分片，D1 仅作五分钟降级存储，公开接口增加 30 秒缓存；修复新建目标首分钟数据因时间粒度不同被过滤的问题。
- Latency Agent 安装与自动更新增加 SHA-256 固定校验和低权限 systemd 运行；删除 Windows Agent、安装脚本和发布产物，Linux 五架构继续本地构建。
- 备份恢复前快照改为加密存储，替换恢复会清理旧运行态，任何中途失败自动还原原数据；NodeQuality 图片代理、存储型输出和普通 Agent Token 权限边界同步加固。
- Rust Agent 更新至 `v1.0.32`：修复依赖安全公告，限制公开响应体积与 HTTPS 重定向，手工更新在服务启动失败时自动恢复上一版。现有 Linux Agent 通过校验后的自动更新获取，无需逐台重新安装。

## 0.24.0-beta.17 - 2026-07-27

- 修复旧 D1 已记录上一版迁移标记时跳过 Ping 与第三方 Latency 曲线颜色字段，导致 `/api/latency` 返回 500、详细图表只显示 Cloudflare 的问题。
- 数据库迁移标记升级并增加旧库回归测试；已有第三方 Latency 历史和节点配置保持不变，升级后自动恢复显示。

## 0.24.0-beta.16 - 2026-07-27

- Web 监控以最近一次检查结果为唯一状态来源；检查成功时稳定显示“运行正常”，不再因没有 Agent 心跳而误报“数据延迟”。
- 外部 Latency Agent 与 TCP Ping 统一采用 `1000ms` 总连接预算，DNS 和最多八个 IPv4/IPv6 地址并行竞争；超时记录为丢包，不再产生 `2000–4000ms` 的伪成功尖峰。现有 Latency 节点会自动升级脚本，无需重新部署。
- Ping 目标和独立 Latency Agent 均可在后台设置曲线颜色；颜色随 D1、备份恢复、Agent 目标下发与公开图表保持一致。
- 修复 LXC 等容器只能看到宿主 DRM 元数据时误显示 GPU 的问题；Agent 只上报可实际访问的 GPU 设备，前端同时兼容清理旧 Agent 的虚假 GPU 标记。
- 系统设置卡片默认折叠，桌面图表指标与时间工具统一右对齐；管理员账号更新新增卡片内进度、失败原因和成功状态，更新后新凭据立即接管登录。
- 修复 Cloudflare 定时触发器受 `10ms` CPU 上限中止后整批目标漏检的问题：Cron 改为 HMAC 签名内部调度，每分钟平滑处理 20 个目标，五分钟覆盖 100 个目标；完整检查不再重复执行快速检查，漏检记录也不再叠加“检查失败”。
- Rust Agent 更新至 `v1.0.31`；现有安装通过校验后的自动更新获取，无需逐台重新安装。

## 0.24.0-beta.15 - 2026-07-27

- 后台新增 VPS 批量设置，可勾选多个节点后统一修改商家、机器类型、到期时间、流量额度、统计方式和独立流量重置日；未勾选的字段保持原值。
- VPS 与 Web 监控统一按每日汇总加权计算最近 30 天 SLA；列表只显示更醒目的百分比，“最近 30 天”完整含义保留在辅助说明中。
- 修复缺少 `dig` 或 `nslookup` 的 VPS 被空兼容入口污染结果、将原生 IPv4 解锁全部误判为 DNS 解锁的问题。
- DNS 兼容查询改由 Agent 内置的受限系统解析器完成，可识别普通解析、无泛解析和 DNS 泛解析；不安装系统软件、不需要 root，也不接受任意命令。报告返回的地区值（包括 `ALISG`）保持原样。
- Rust Agent 更新至 `v1.0.30`；现有安装可通过版本、manifest 与二进制 SHA-256 验证后自动升级，无需重新部署。升级后需手动重新运行一次 IPv4 解锁，以替换数据库中的旧检测结果。

## 0.24.0-beta.14 - 2026-07-27

- IPv4 解锁状态格由深色改为浅绿、浅红、浅黄与浅灰蓝，并使用对应深色文字，减轻视觉重量同时保持小字号清晰可读。
- 延续 Beta 13 的 VPS 标题、GeoIP 和 Latency 列精简：商家与节点名称合并显示，粗粒度探测区域与列表外部 Latency 信息不再出现。
- 修复宽屏图表工具栏从左侧裁掉 `Latency` 或 `CPU` 标签的问题；指标栏从完整的第一个标签开始显示，空间不足时只在右侧横向滚动。
- 桌面与 320px 移动端实测保持两行六列布局，无横向溢出；Rust Agent 保持 `v1.0.29`，无需重新安装。

## 0.24.0-beta.13 - 2026-07-27

- VPS 标题统一为“商家名称 + 节点名称”，节点名称已经包含商家时自动去重；原独立商家徽标不再重复显示。
- VPS 列表移除 `亚太`、`北美西部`、`东欧` 等 Cloudflare 探测区域，仅保留 Agent 根据出口 IP 上报的 GeoIP 国家与城市。
- VPS 列表右侧不再展示外部 Latency Agent 名称与延迟，只保留无名称的 Cloudflare 延迟数值；点开节点后的详细图表继续保留外部 Latency 历史曲线。
- IPv4 解锁重新整理为紧凑两行六列：服务名称位于第一行，第二行使用深绿、深红、深黄状态格并显示两位国家代码。
- 桌面和移动端同步压缩 VPS 信息列；320px 小屏实测无横向溢出，长商家/节点名称与状态列不会互相遮挡。
- Rust Agent 保持 `v1.0.29`，本次无需重新安装或更新 Agent。

## 0.24.0-beta.12 - 2026-07-27

- TCP Ping 改为每个解析地址 `1000ms` 严格超时，IPv4/IPv6 最多八个地址并行竞争，只记录最快的真实建连耗时；失败回退等待不再累加成 `2000–4000ms` 的伪成功尖峰。
- 每个 TCP Ping 目标新增独立丢包率；超时统一按丢包记录，前端在图表上方显示对应目标的统计结果。
- 修复一分钟 R2 快速状态时间覆盖 D1 五分钟持久检查时间后，历史检查被持续推迟并显示漏检的问题；历史补点与调度统一使用持久化检查时间。
- Agent 固定 Beta 任务空闲轮询降至每五分钟一次，减少 Worker 请求消耗；后台同步提示最长领取时间。
- 移动端专项优化 VPS 行、图表、指标标签、分页、NQ 弹窗与触控尺寸；IPv4 解锁固定为单行六格并放在运行时长与 SLA 格子之间。
- 普通 VPS 不再因历史序列中的 `gpu_util: 0` 误显示 GPU，真实检测到 GPU 名称或数量时仍正常展示。
- Rust Agent 更新至 `v1.0.29`；现有 Agent 可通过验证更新自动升级，无需重新安装。

## 0.24.0-beta.11 - 2026-07-26

- 真实中国大陆 VPS 回归发现节点无法访问 GitHub Release，导致 `v1.0.27` 的临时 jq 下载失败；现将五架构 jq 官方固定产物随 Worker 静态资源和 Agent Release 一起发布。
- Agent 优先从自身已经连通的 NIE-SLA Worker `/bin/` 下载对应 jq，GitHub 官方 Release 仅作备用源；两条路径都必须通过代码内固定 SHA-256 才会执行。
- 一键部署构建会验证并打包 amd64、arm64、i386、armhf 与 armel 五个 jq 产物，确保新部署和自建域名同样具备兼容资源。
- 后台操作列继续采用 Beta 10 的常规管理/Beta 任务分组、对齐和窄屏换行布局。
- Rust Agent 更新至 `v1.0.28`；现有 Agent 可自动升级，无需重新安装。

## 0.24.0-beta.10 - 2026-07-26

- 真实低权限 VPS 回归发现 IP.Check.Place 即使跳过依赖安装，仍会因系统缺少 `jq` 在检测初期退出；Agent 现仅在缺失时下载 jq 官方固定版本。
- jq 按 Agent 架构选择 amd64、arm64、386、armhf 或 armel 产物，并核对写死的 SHA-256；文件只进入 `0700` 任务私有目录，任务结束自动删除，不安装软件、不修改系统且不需要 root。
- 后台探针操作列重新分组：部署、禁用、编辑、删除保持为常规操作，Beta 标识、运行模式、NQ/IP 解锁与任务状态形成独立区域，窄屏下可自然换行。
- 继续固定 `-n -p`，禁止 `sudo`、系统包安装和第三方报告上传；最终仍只保存 IPv4 媒体解锁字段。
- Rust Agent 更新至 `v1.0.27`；现有 Agent 可自动升级，无需重新安装。

## 0.24.0-beta.9 - 2026-07-26

- 真实低权限 VPS 回归确认 IP.Check.Place 会在媒体检测完成后因缺少可选 `dig` 中断，无法生成最终 JSON；Agent 现于任务私有目录提供一次性 DNS 兼容入口，只让与结果无关的 DNSBL 阶段继续完成。
- 兼容入口随任务目录删除，不写系统目录、不安装依赖、不修改 VPS 软件包，也不需要 root；最终只保存媒体解锁字段，不采用 DNSBL 或邮件检测结果。
- JSON 解析改为从广告、进度和错误混合输出中识别完整结构化对象，避免非 JSON 花括号干扰。
- 固定保留 `-n -p`，继续禁止 `sudo`、系统包安装和第三方报告上传。
- Rust Agent 更新至 `v1.0.26`；现有 Agent 可自动升级，无需重新安装。

## 0.24.0-beta.8 - 2026-07-26

- IP 解锁恢复 IP.Check.Place 官方 `-j` 结构化输出，Agent 直接提取最终媒体解锁字段，避免终端表格受字体、语言或输出格式影响。
- 固定保留 `-n -p`：不执行依赖安装、不调用 `sudo/apt`、不上传第三方报告，兼容低权限 Agent。
- 即使脚本因缺少可选 `dig` 返回非零，只要 JSON 中已有有效媒体结果就会保存；IPv4 不可用或无有效结果时仍然失败。
- Rust Agent 更新至 `v1.0.25`；现有 Agent 可自动升级，无需重新安装。

## 0.24.0-beta.7 - 2026-07-26

- 真实最小化 VPS 回归发现 IP.Check.Place 会在媒体检测完成后因缺少可选 `dig` 返回非零；Agent 现改为先解析固定媒体表，再判断退出码。
- 只要得到至少一行有效媒体解锁数据就保存结果；IPv4 不可用或没有有效媒体行时仍然失败，不会把空结果标成成功。
- NodeQuality 继续严格要求脚本成功退出，本次容错只作用于 IP 解锁的固定结果解析。
- Rust Agent 更新至 `v1.0.24`；现有 Agent 可自动升级，无需重新安装。

## 0.24.0-beta.6 - 2026-07-26

- 真实兼容节点回归发现 IP.Check.Place 的 `-y` 会在缺少可选依赖时尝试 sudo；现改用官方 `-n` 参数跳过系统检查和依赖安装。
- IP 解锁固定启用 `-p` 隐私模式，不向第三方上传报告；Agent 直接解析脚本底部媒体解锁表，不依赖脚本生成 JSON。
- 继续保留固定 HTTPS 来源、固定参数、清空环境、私有一次性目录、超时与输出上限，且低权限兼容模式不要求 root。
- Rust Agent 更新至 `v1.0.23`；现有 Agent 可自动升级，无需重新安装。

## 0.24.0-beta.5 - 2026-07-26

- 修复兼容模式 Agent 领取 IP 解锁任务后误用 root Manager 目录，导致任务在脚本启动前立即失败的问题。
- 低权限 IP 解锁改用 Agent 自身状态目录中的隔离任务沙箱，不要求 root；root Manager 仍使用独立的 root 专用目录。
- 两种任务目录都校验进程所有权和私有权限、拒绝符号链接，脚本 HOME 也限制在一次性隔离目录中。
- Rust Agent 更新至 `v1.0.22`；现有 Agent 会沿用已经恢复的验证更新策略自动升级，无需重新安装。

## 0.24.0-beta.4 - 2026-07-26

- 修复单 Worker 部署中更新策略反向请求自身静态域名，被 Cloudflare 拦截后缺少 Agent 版本、下载地址与校验和的问题。
- 内置 Agent 更新元数据改为直接读取 Static Assets 绑定；显式配置外部下载源时仍按原方式读取。
- 更新策略新增 `release_ready` 状态并记录元数据读取错误，避免接口静默返回不完整的成功响应。
- Rust Agent 保持 `v1.0.21`；旧 Agent 收到完整策略后会沿用现有定时更新通道升级，无需重新安装。

## 0.24.0-beta.3 - 2026-07-26

- 修复升级到 Beta 2 时复用旧 D1 的安装缺少 Agent 能力列，导致全部 Agent 上报中断和后台误判离线的问题。
- Schema 标记升级到 v17，现有安装会自动补齐列；Agent 上报在迁移未完成时也会自愈并重试。
- 后台 Agent 列表增加旧 Schema 兼容读取，数据库升级期间仍会保留版本与最后上报时间。
- Rust Agent 保持 `v1.0.21`，无需重新安装。

## 0.24.0-beta.2 - 2026-07-26

- 将兼容服务升级为常驻 root Manager，统一负责固定动作、验证更新、服务维护与能力心跳；以后增删动作不再新增 VPS 服务。
- Agent 主进程提供仅允许 `ip_unlock` 的低权限兼容模式；Manager 心跳正常时不会重复领取任务或增加请求量。
- Worker 保存经过白名单清洗的 Agent 能力，后台按每台 VPS 的真实能力启用按钮，并拒绝向离线或不支持的 Agent 排队。
- 固定脚本执行不再拼接 shell 命令，清空 Agent 环境并限制地址、参数、下载大小、运行时间和输出。
- Agent 更新增加独立稳定性 watchdog；新 Manager 或低权限遥测进程未在观察期内保持稳定时，自动恢复上一版并重启服务。
- 已有 Manager、root 更新任务或 root 主进程的旧安装会自动迁移；只有确实没有 root 通道的节点需要一次修复。
- Rust Agent 更新至 `v1.0.21`。

## 0.24.0-beta.1 - 2026-07-26

- 新部署改为单 Worker：Static Assets、API、D1、R2、Durable Objects 与每分钟 Cron 一次完成。
- 首次公开访问会自动初始化 D1 Schema，避免一键部署后等待 Cron 期间短暂返回 500。
- 保持旧 Agent API 协议、Target ID 和 scoped Token 兼容，支持从 Pages + Worker 复用原 D1/R2 迁移。
- 国家与城市不再手工选择，由 Agent 查询出口 IPv4/IPv6；后台可选 IP.SB、Cloudflare、IPIP.net 或自定义 HTTPS JSON。
- 商家目录、自定义商家和现有机器类型继续保留。
- 新增两个只能手动点击的 Beta 动作：NodeQuality 固定输入 `v/y/y/y`，IPv4 解锁只保存最终媒体解锁字段。
- Linux 新增独立 root 任务 runner；主遥测 Agent 继续低权限运行，接口不接受任意命令、参数、脚本 URL 或 stdin。
- 新安装默认开启已签名 Agent 自动更新；systemd timer 与 OpenRC hourly job 每小时检查，只允许语义版本升级并拒绝自动降级。
- 新增普通备份、密码保护敏感备份、恢复预览、合并/替换恢复与恢复前 R2 快照。
- 保留 Telegram、邮件、后台更新与内置外观配置。
- 移除主题、插件上传、市场导入和包运行时；公开 `/api/v1` 只读接口继续支持第三方前端。
- Rust Agent 更新至 `v1.0.20`，并由可信本地环境生成六架构 release 产物。

## 0.23.0-beta.2 - 2026-07-26

- 内测阶段曾提供手工国家/城市目录，现已由 0.24 的 Agent 自动 GeoIP 取代。

## 版本规则

- `app-vX.Y.Z`：Worker 与同源前端源码 Tag。
- `vX.Y.Z`：Rust Agent 二进制与 Release。
- 两类 Tag 的版本数字必须一致；正常迭代共同增加补丁位 `0.0.1`。
