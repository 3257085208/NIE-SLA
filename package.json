{
  "name": "nie-sla-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NIE-SLA Cloudflare 一键部署",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/prepare-one-click.mjs",
    "test": "node tests/public-repo-safety.test.mjs && node tests/one-click-build.test.mjs",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy",
    "check:deploy": "npm run build && npm test && wrangler deploy --dry-run --outdir .wrangler-dry-run"
  },
  "dependencies": {
    "fflate": "0.8.2"
  },
  "devDependencies": {
    "wrangler": "4.88.0"
  },
  "cloudflare": {
    "label": "NIE-SLA 状态页与 VPS 探针",
    "products": [
      "Workers",
      "D1",
      "R2",
      "Durable Objects"
    ],
    "categories": [
      "storage"
    ],
    "docs_url": "https://github.com/3257085208/NIE-SLA/blob/main/docs/zh-CN/02-deployment.md",
    "publish": true,
    "bindings": {
      "ASSETS": {
        "description": "前端静态文件绑定，由构建流程自动生成，无需修改。"
      },
      "DB": {
        "description": "保存配置、状态、SLA、会话和告警数据的 D1 数据库；Cloudflare 会自动创建并绑定。"
      },
      "ARCHIVE": {
        "description": "保存 Agent 高频指标、Ping 历史和状态快照的 R2 存储桶；Cloudflare 会自动创建并绑定。"
      },
      "REGION_PROXY": {
        "description": "执行可选区域探测的 Durable Object；Cloudflare 会自动配置，无需填写。"
      },
      "ADMIN_USERNAME": {
        "description": "必填：后台管理员账号，使用 3-64 位字母、数字或 . _ @ -。"
      },
      "ADMIN_PASSWORD": {
        "description": "必填：后台管理员密码，请使用至少 20 位且不与其他服务复用的随机密码。"
      },
      "TOTP_ENCRYPTION_KEY": {
        "description": "必填：用于加密 TOTP 与节点 Token 明文副本，请使用至少 32 字节的独立随机值。"
      }
    }
  }
}
